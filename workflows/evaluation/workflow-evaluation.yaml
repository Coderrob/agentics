name: workflow-evaluation
category: evaluation
description: Benchmark optimization outcomes after refinement updates.
inputs:
  baseline_usage:
    type: object
  candidate_usage:
    type: object
outputs:
  benchmark_report:
    type: object
steps:
  - name: compare
    run: echo "Compare token usage, tool call count, execution time, and success rate"
